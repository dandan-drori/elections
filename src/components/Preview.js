import React from "react";
import styled from "styled-components";

const Preview = () => {
  return <Container>Chart Preview</Container>;
};

export default Preview;

const Container = styled.div`
  color: white;
  margin-top: 5rem;
`;
